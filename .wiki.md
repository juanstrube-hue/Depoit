# Project Summary
The project is a modern fintech web application designed to facilitate contract management, deposit returns, and notifications for users in various roles, including landlords, tenants, and brokers. It leverages a robust backend for data management and a responsive frontend for user interaction, aiming to streamline financial transactions and enhance user experience.

# Project Module Description
- **Backend**: Handles data storage, authentication, and business logic using a structured API.
- **Frontend**: Provides a user-friendly interface with components for navigation, authentication, and content display.
- **Database**: Stores user profiles, contract details, and transaction records.

# Directory Tree
```
app/
├── backend/                # Backend service files
│   ├── core/               # Core functionalities and configurations
│   ├── data_models/        # JSON data models for contracts and notifications
│   ├── dependencies/       # Dependency injection and database connections
│   ├── models/             # Data models for ORM
│   ├── routers/            # API route definitions
│   ├── services/           # Business logic and service layer
│   ├── schemas/            # Data validation schemas
│   ├── main.py             # Entry point for the backend application
│   └── requirements.txt     # Python dependencies
└── frontend/               # Frontend service files
    ├── src/                # Source code for the React application
    ├── public/             # Static assets
    ├── package.json        # Node.js dependencies
    └── vite.config.ts      # Vite configuration
```

# File Description Inventory
- **app/backend/main.py**: Main entry point for the backend application.
- **app/frontend/src/App.tsx**: Main component of the React application.
- **app/backend/requirements.txt**: Lists Python dependencies required for the backend.
- **app/frontend/package.json**: Lists Node.js dependencies required for the frontend.

# Technology Stack
- **Backend**: Python, FastAPI, SQLAlchemy
- **Frontend**: React, TypeScript, Vite
- **Database**: PostgreSQL
- **Styling**: Tailwind CSS

# Usage
To install dependencies, build, and run the application:

1. **Backend**:
   - Navigate to the backend directory: `cd app/backend`
   - Install dependencies: `pip install -r requirements.txt`
   - Run the application: `python main.py`

2. **Frontend**:
   - Navigate to the frontend directory: `cd app/frontend`
   - Install dependencies: `npm install`
   - Build the application: `npm run build`
   - Start the application: `npm run dev`
